@model YonazoneWorkForce.ViewModels.AllEmployeesViewModel

<h2>@ViewData["Title"]</h2>
<br>

@foreach (var e in Model.Employees)
{
    <div class="row">
        <p class="col-lg-3">@Html.DisplayFor(modelItem => e.FirstName) @Html.DisplayFor(model => e.LastName)</p>
    </div>
}
